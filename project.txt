This is my first file that i am going to code for a project . I will track it.
using git > then i will commit ( Saving  it Local git repo) it. Then i will push it to server from where it can be shared to anyone.

1 - Create a file ( Workspace)
 
2 - Untill unless i dont save it - this file location will be termed as ' File is residing in GIT staging area.

3- To make GIT track this particular file only my desktop, when i have many files already.
git add filename - make git track this particular file only.

4 - I will save this to local git repo using the below command :
 git commit -m " Any message"

5 - Then we pusj it to the remote repository(server)
